# 2026-04-24 · 架构重构 6 步全部完成

## 一句话总结

主原型 `创建需求-上传页面.html` 从 **9883 行单文件**重构到 **1123 行入口** + **39 个外部资源文件**，体积减少 88.6%，零功能回归，零运行时错误。

## Step 完成清单

| Step | 内容 | 产物 | 状态 |
|---|---|---|---|
| 0 | 备份 + 目录骨架 + AI 必读文档 | 7 目录 + 3 份 docs | ✅ |
| 1 | CSS 拆分（4900 行 → 17 个文件） | `styles/` | ✅ |
| 2 | 数据层抽离（21 个常量 → 10 个文件） | `scripts/data/` | ✅ |
| 3 | core 基础设施（icons/toast/router/utils/livereload） | `scripts/core/` | ✅ |
| 4 | 页面 JS 模块化（~3000 行 → 7 个 page 文件 + main.js） | `scripts/pages/` | ✅ |
| ~~5~~ | HTML partials 抽离 | 暂不做（入口已经够小） | ⏭️ 跳过 |

> Step 5 决定不做：当前 1123 行入口主要是 HTML 模版（视图骨架），抽出去会增加 fetch 注入复杂度，得不偿失。如果未来加新页面，按 `docs/ADD-NEW-PAGE.md` 直接在 partials/ 下新建即可。

## 最终目录结构

```
/Users/mason/Desktop/Cursor/
├── 创建需求-上传页面.html          1123 行（入口）
├── 创建需求-上传页面.html.backup   9883 行（保命备份）
├── dev-server.py                   多线程版（已修过单线程被 livereload 拖死的 bug）
│
├── styles/  (17 个 .css)            → 详见 docs/ARCHITECTURE.md
├── scripts/
│   ├── core/  (5 个 .js)           icons / toast / router / utils / livereload
│   ├── data/  (10 个 .js)          所有 mock 数据，接 API 时只改这里
│   ├── pages/ (6 个 .js)           topbar / wizard / result / demand-list / copy-list / ai-chat
│   └── main.js                     DOMContentLoaded 入口
│
├── docs/                            ← AI 必读
├── 产品文档/                        ← 你的工作区
└── 原型预览/                        ← 其它静态原型
```

## 每个页面对应文件（一查就知道改哪里）

| 你想改 | 改这里 |
|---|---|
| 工作台样式 | `styles/pages/workbench.css` |
| 工作台逻辑 | (主要在 partials 内嵌 onclick + topbar.js 的 fillStaticIcons；workbench 没单独 page JS，因数据简单纯 HTML 渲染) |
| 文案管理列表样式 | `styles/pages/copy-list.css` |
| 文案管理列表逻辑 | `scripts/pages/copy-list.js` |
| 文案管理 mock 数据 | `scripts/data/copy-list.js` |
| 文案管理新增列 | 改 HTML 模版（入口）+ `scripts/pages/copy-list.js` 的 `renderCopyListTable` |
| AI 对话页 | `styles/pages/ai-chat.css` + `scripts/pages/ai-chat.js` + `scripts/data/ai-skills.js` / `bg-knowledge.js` 等 |
| 全局主色 | `styles/_tokens.css` 的 `--primary` |
| 加新图标 | `scripts/core/icons.js` 的 `ICON_PATHS` |

## 资源加载顺序（`<head>` 内严格保证）

```
1. CSS link              17 个文件
2. Mock 数据             10 个文件（必须最先，因为有顶层立即引用）
3. Core 基础设施          4 个（icons/utils/toast/router；livereload 在 body 末尾）
4. 页面级 JS              6 个
5. main.js               DOMContentLoaded 入口
─────────────
6. </body>
7. livereload.js          最后加载，依赖最少
```

## Bug 修复记录（重构中发现的 3 个 bug）

1. ✅ [文案管理列表初次点击空白](../03-讨论决策/2026-04-24-bugfix-文案管理列表初次点击空白.md) — flex 布局塌陷，根因是 `display:block` 覆盖了 CSS 中的 `display:flex`
2. ✅ [dev-server 单线程被 livereload 拖死](../03-讨论决策/2026-04-24-bugfix-dev-server单线程被livereload拖死.md) — 加 `ThreadingMixIn` 一行解决
3. ✅ Step 4 抽 demand-list 时漏了 `onRowClick` 函数末尾的 `}` — 修正边界 2882→2884

## 给你的建议

### 短期（这周）
- 体验一下「改样式找文件」的效率提升 —— 用脚一定要直接进 `styles/pages/{页面}.css`
- 加新功能时严格遵守 [`docs/CONVENTIONS.md`](../../docs/CONVENTIONS.md) 的命名规则
- 任何新页面按 [`docs/ADD-NEW-PAGE.md`](../../docs/ADD-NEW-PAGE.md) 的 5 步流程

### 长期（半年内）
- 当某个 page 文件超过 800 行时，按 `docs/CONVENTIONS.md` 拆子文件
- 如果接 API：只改 `scripts/data/` 下对应文件，render 函数无需改
- 如果接团队：可平滑升级到 ES Modules 或 Vite + Vue（架构方案 B/C）

## 数字盘点

| 指标 | 重构前 | 重构后 | 变化 |
|---|---|---|---|
| 入口 HTML 行数 | 9883 | 1123 | **-88.6%** |
| 文件数 | 1 | 40 (1 入口 + 39 外部) | +39 |
| CSS 行数 | 4900 (内嵌) | 4979 (跨 17 个文件) | ≈ 等量 |
| JS 行数 | ~4000 (内嵌) | ~3650 (跨 22 个文件) | ≈ 等量 |
| 「找一段代码」成本 | 滚 9000+ 行 | 直接进对应文件 < 800 行 | **10× 快** |
| 多人/多 AI 协作冲突风险 | 高 | 低 | ✓ |
| 未来加新页面成本 | 在 9000 行里见缝插针 | 复制三件套即可 | ✓ |

---

📅 2026-04-24 · 一日重构完成
👤 Mason + AI（Claude Opus 4.7）
🎯 状态：✅ 完成
🔗 相关文档：
- [架构重构方案](../03-讨论决策/2026-04-24-架构重构方案.md)
- [docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md)
- [docs/CONVENTIONS.md](../../docs/CONVENTIONS.md)
- [docs/ADD-NEW-PAGE.md](../../docs/ADD-NEW-PAGE.md)
